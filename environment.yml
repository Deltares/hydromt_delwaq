name: hydromt-delwaq

channels:
  - conda-forge

dependencies:
  - cartopy # to run examples
  - descartes # to run examples
  - hydromt>=0.9.1,<0.11
  - hydromt_wflow>=0.5.0
  - hydromt_delwaq>=0.3.0
  - jupyter # to run examples
  - matplotlib # to run examples
  - numpy<2
  - python<=3.11
  - tqdm # progress bar
  - xugrid
